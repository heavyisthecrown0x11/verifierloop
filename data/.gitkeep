# Period-harvest root. At runtime the loop writes per-period artifacts under
# data/periods/<N>/ following the `contract` crate's file layout. Contents are
# gitignored; this file only keeps the directory in the tree.
